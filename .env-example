# Django configuration
DEBUG=True
SECRET_KEY=django-insecure-dev-key-change-in-production

# Database settings
DB_NAME=agritech
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Docker settings
IN_DOCKER=True

# Local development settings
USE_POSTGRES=True
LOCAL_DB_HOST=localhost

# Port configuration
BACKEND_PORT=8001
FRONTEND_PORT=4000

# CORS settings
ALLOWED_HOSTS=localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=http://localhost:4000,http://frontend:3000,http://localhost:3000

# Frontend settings
NEXT_PUBLIC_API_URL=http://localhost:8001 